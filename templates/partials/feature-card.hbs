{{#unless inSheet}}<div class="legends">{{/unless}}
<section class="item feature" data-item-id="{{_id}}">
  <h3 class="title">
    {{#if inSheet}}
    <a class="item-roll black menu">{{name}}</a>
    {{else}}
    {{name}}
    {{/if}}
  </h3>
  {{{system.description}}}
</section>
{{#unless inSheet}}</div>{{/unless}}
